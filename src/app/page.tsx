import Header from '@/components/Header';
import ThemeContainer from '@/components/ThemeContainer';

const Home = () => {
  return (
    <ThemeContainer>
      <Header />
    </ThemeContainer>
  );
};

export default Home;
